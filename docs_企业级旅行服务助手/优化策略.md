# 企业智行 · 优化策略

> 创建：2026-08-28 · 基于 `docs/优化策略.md` 执行。与 `docs/PROGRESS.md` 主线对齐：先保稳定可演示，再打通 P1，再渐进治理技术债。
> **批次 1 已于 2026-08-28 完成**（见下文「二、批次 1 修复记录」）。

---

## 一、现状诊断（一句话）

P0 功能完整可演示，P1 代码超前但断链；批次 1 已消除 5 个高危问题（事件循环阻塞、鉴权后门、并发竞态、LLM 错误污染、存储非原子写），剩余工作为 P1 链路打通 + 渐进式技术债治理。

---

## 二、批次 1 修复记录（✅ 已完成，2026-08-28）

| # | 问题 | 修复 | 涉及文件 |
|---|---|---|---|
| 1 | journey-hub 在 `async def` 中同步执行 LangGraph+LLM，单请求卡死整个服务（含 /health） | 非流式对话路由改普通 `def`（FastAPI 自动进线程池） | `services/journey-hub/api/main.py` |
| 2 | 鉴权后门：`ak_dev_local` / `ak_test_*` 在所有环境放行；`?api_key=` 查询参数泄入日志 | 新增 `APP_ENV` 判定，后门 key 与 query 传 key 仅 dev 生效，`APP_ENV=production` 即关闭 | `shared/middleware/auth.py` |
| 3 | 行程 IDOR：知道 trip_id 即可读写任意行程 | trips 及子资源（详情/编辑/删除/重排/清单/总结/费用/存模板）加 `_deny_if_not_owner` 属主校验；声明身份且不符时按 404 拒绝；未声明身份兼容放行（P2 SSO 后改强制） | `services/planner-core/api/main.py` |
| 4 | 生成器并发竞态：全局改 `itinerary_gen.model`、共享 `_last_trip` 导致并发串单 | 每次请求创建独立 `ItineraryGenerator` 实例 | `services/planner-core/api/main.py` |
| 5 | LLM 流式失败时把错误文本当正文 chunk yield，混进行程 JSON 导致下游解析失败 | `chat_stream` 改抛 `LLMError`；生成器记录 `last_error` 由 API 层发独立 error 帧；`chat_json` 坏输出重试 1 次后抛 `LLMError` | `shared/llm/manager.py`、`shared/llm/__init__.py`、`services/planner-core/generators/itinerary.py` |
| 6 | BaseJsonStore 非原子写（崩溃即损坏）、无锁 | temp 文件 + `os.replace` 原子写 + `threading.RLock` 保护 save/update/delete | `shared/store/base_store.py` |

**验证情况**：`py_compile` 全过；pytest 24 passed；`APP_ENV=production` 下后门 key 确认拒绝；存储 50 条并发写无 `.tmp` 残留、文件数正确；`chat_json` 对不可达端点抛 `LLMError`（含一次重试）。端到端脚本 `scripts/e2e_verify.py` 覆盖：SSE 生成、属主校验（本人 200/他人 404/匿名兼容）、对话期间 /health 响应、并发生成不串单。

**遗留注意**：
- 生产部署必须在 `.env` 加 `APP_ENV=production`（当前默认 dev，保留演示便利）。
- `.env` 含真实 key 在项目根目录，项目不是 git 仓库——建议尽早 `git init` 并把 `.env` 入 `.gitignore`。

---

## 三、后续优化步骤（按批次执行）

### 批次 2：P1 链路打通 + god file 拆分（✅ 已完成，2026-08-29）

按 PROGRESS.md 待办顺序，**先拆文件再接链路**，避免继续往 1,181 行的 main.py 堆代码：

- [x] **B2-1 拆分 planner-core god file**（✅ 2026-08-29）
  - `api/main.py` 从 ~1,200 行减到 ~140 行装配层；共享实例移入 `api/deps.py`；路由拆为 `api/routers/{trips,profiles,templates,org,policy,policy_docs,approvals}.py`
  - 顺带修复存量 bug：`GET /policies/match`、`POST /policies/check` 原被 `/policies/{policy_id}` 路径参数吞掉，已在 router 内调整注册顺序
  - 顺带修复存量 bug：`.env` 相对数据路径（`TRIP_DATA_DIR=./data/trips`）以服务目录为 cwd 解析，实际写到 `services/planner-core/data/` 形成数据孤岛；`settings.py` 增加 `_anchor_data_path` 将相对数据路径锚定到项目根，并把服务侧存量 36 个数据文件（29 行程 + 组织 + 政策文档）完整迁移合并到根 `data/`（校验一致后清除服务侧副本）
  - 回归：24 pytest passed；31 条 API 路径全部注册；服务从根 data/ 加载 29 条行程
- [x] **B2-2 journey-hub 假流式改真流式**（✅ 2026-08-29）
  - `graph.py` 新增 `invoke_stream`：chat 意图直接 `chat_stream` 逐 chunk 透传（复用 chat_query 的 prompt 组装与会话历史）；plan/manage/emergency 意图走完整图后整段返回
  - 事件契约：新增 `chunk` 帧，保留最终 `respond` 整帧兼容旧契约；`ChatPage.jsx` 增加 chunk 增量渲染
- [x] **B2-3 + B2-4 行程生成 → 政策检查 + 审批自动发起**（✅ 2026-08-29）
  - `api/routers/trips.py` 新增 `_post_generate_policy_flow`：行程保存后在 SSE 流尾部追加 `policy` 帧（违规明细）与 `approval` 帧（自动发起的审批单）
  - 员工解析：session_id 恰为员工 ID 时按职级匹配政策（前端默认会话 `web-user` 已做种子员工，演示直接命中）；审批人取直属主管 `manager_id`；链路失败仅告警不阻断行程生成
- [x] **B2-5 P1 种子数据**（✅ 2026-08-29）
  - `scripts/seed_p1_data.py`（幂等，可重复运行）：2 部门 / 3 员工（含 `web-user` 张伟、主管 `emp_1001` 李明远）/ 2 政策（junior 600 元住宿+须审批、director 1200 元）/ 3 政策文档（RAG）/ 2 条待审批示例

**批次 2 验证结果**（`scripts/e2e_verify.py` 六项 + 单测）：
- pytest 24 passed；31 条 API 路径全注册；服务从根 `data/` 加载 29 条行程
- ① SSE 生成 12.5s 出完整行程（含 `policy` 帧）② 属主校验 本人 200/他人 404/匿名兼容 ③ 对话期间 /health 最慢 28ms ④ 并发生成不串单 ⑤ 政策检查帧 + 自动审批单 1 条关联本次行程 ⑥ 真流式 153 个 chunk 帧、首个 1.3s 到达（PRD 指标 3s）
- 注：E2E 测试查询需避开「去/玩/旅游」等规划关键词，否则被意图路由判成 plan 走整图路径（无 chunk 帧，属预期行为）

### 批次 3：测试补齐 + 前端工程化 + 审批门禁（✅ 已完成，2026-08-29）

- [x] **B3-0 审批门禁（PRD 3.5 闭环）**（✅ 2026-08-29，用户确认加急实现）
  - 生成时自动发起审批的行程 → `status=pending_approval`；主管通过 → `planned`（生效）；拒绝 → `cancelled`
  - 门禁点：`pending_approval` 的行程不允许应变重排（409）
  - 顺带修复真 bug：审批单创建时未写 `status` 字段，导致审批人列表（按 pending 过滤）看不到、也无法审批——已在自动链路、手动创建接口、种子脚本三处补齐，存量数据由种子脚本幂等迁移
  - 验证：生成→待审批→审批人可见→通过→生效 全链路 PASS
- [x] **B3-1 shared 层测试**（✅ 2026-08-29）：`tests/` 新增 auth（后门环境收窄 5 例）/ base_store（原子写、并发 50 写无损坏、坏文件跳过 6 例）/ llm（chat_json 重试、流式错误不混入正文 6 例）/ http_client（SSE 帧解析、鉴权头 5 例）
- [x] **B3-2 reroute 算法测试**（✅ 2026-08-29）：时间工具、前移/顺延/重排三算法、变更类型识别、闭园替代/延误顺延/未匹配 no-op 共 9 例
- [x] **B3-3 API 集成测试**（✅ 2026-08-29）：FastAPI TestClient + 临时数据目录隔离，覆盖生成全生命周期、属主越权、policies/match 回归、审批校验、模板复用、坏 key 403 共 9 例；总计 **62 passed**（原 24）
  - 踩坑记录：三个服务都有顶层 `api` 包，扁平 sys.path 下解析顺序脆弱——测试中必须无条件把目标服务路径挪到 sys.path[0]（`if not in` 守卫会被其他服务测试的插入顶掉）
- [x] **B3-4 前端 API 去重**（✅ 2026-08-29）：新增 `api/client.js`（request/post/put/del/ssePost 公共封装），journey/planner/sense/organization 四模块全部改为复用，SSE 解析循环收敛为一处
- [x] **B3-5 修前端 401 bug**（✅ 2026-08-29）：`organization.js` 默认 key `'dev-key-2024'` 与后端不一致（组织页 401），随 client.js 统一修复，实测组织接口 200
- [x] **B3-6 前端代码分割**（✅ 2026-08-29）：7 个页面 React.lazy + Suspense，构建产物 7 个页面 chunk 独立（4.5~14.5KB each）
- [x] **B3-7 配置治理**（✅ 2026-08-29）：journey-hub 全部改走 `shared.config.settings`（新增 `planner_service_url`/`sense_service_url` 字段）；删除死配置 `REDIS_URL`（settings + .env.example）；requirements 加版本上界（以当前已验证版本为准）

**批次 3 回归**：pytest 62 passed；E2E 六项全过（真流式 176 chunks / 首字 1.2s）；前端 lint 0 error、build 正常、组织接口 200。

### 收尾优化（✅ 2026-08-29，原 B3-8/B3-9 可选项）

- [x] **重复代码收敛**：`http_client.py` get/post/put/delete 四方法抽统一 `_request`；UTF-8 charset 修复逻辑（llm/manager × 2 + http_client × 1 三处重复）统一为 `ensure_utf8_encoding`；SSE 响应样板抽 `shared/sse.py`（`sse_frame` / `SSE_DONE` / `sse_stream_response`），journey-hub 与 planner 两处 StreamingResponse 样板收敛；journey-hub `/agent/sessions` 直访 `session_manager._sessions` 私有属性改为公共 `list_sessions()` 方法
- [x] **HTTP 请求级 metrics 中间件**：`app_factory` 统一注入，请求计数 + 延迟直方图，路径用路由模板（如 `/trips/{trip_id}`）避免高基数标签，`/metrics` 自抓取不计
- [x] **emergency 数据外置**：312 行硬编码应急数据（电话/证件/医院/保险）外置 `tools/emergency_data.json`，emergency.py 312→185 行，运营更新数据不再需要改代码
- [x] **脏数据处理**：历史乱码行程（charset bug 时期）用 latin-1→UTF-8 roundtrip **修复而非删除**，标题/目的地/日期全部还原；删前端空 hooks 目录
- [x] 测试同步：http_client 测试 mock 点从 `session.get` 改为 `session.request`（匹配重构后实现）

**回归**：pytest 62 passed；E2E 六项全过。

### 不做（避免过度工程）

- ❌ 全面异步化（httpx 改造）——线程池已解决阻塞
- ❌ Redis/SQLite 替换文件存储——原子写已够演示用
- ❌ OpenTelemetry 全套——HTTP metrics 中间件先顶住
- ❌ SSO/多租户——P2 范围，属主校验已上最小版本

---

## 四、验收出口标准

| 批次 | 出口标准 | 状态 |
|---|---|---|
| 1 | 并发生成不串单；后门 key 生产模式拒绝；原子写无损坏；LLM 错误不混入正文 | ✅ 完成 |
| 2 | 拆分后全链路回归通过；生成行程自动出政策提示与审批单；P1 页面有数据 | ✅ 完成 |
| 3 | shared 层核心模块有测试；前端无 401、首屏分包 | ⏳ 待办 |
