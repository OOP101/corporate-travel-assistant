# 企业智行 · Corporate Journey Hub · 项目文档

> 本文档为项目唯一权威文档，覆盖背景、架构、进度、踩坑、数据口径全维度。
> 进度主线见下文「七、目前进度」与关键里程碑；简历权威定稿见 [`docs/简历面试/简历项目经历-企业智行.md`](简历面试/简历项目经历-企业智行.md)；出行要素产品规划见 [`docs/PRD.md`](PRD.md)。

---

## 一、项目概述

面向企业内部的智能行程规划与安排平台，以 LLM 为大脑、以多源实时数据为感知，覆盖「行前规划 → 行中保障 → 行后归档」全链路，服务于员工差旅、客户拜访、会议参展、团队出行等企业内部行程场景。

三个松耦合服务（行智 Journey Hub / 策程 Planner Core / 感知 Sense Engine）+ shared 公共层，**单体模式（默认）单进程单端口 8001，一键切换微服务模式**。

---

## 二、项目背景

企业行程安排分散在邮件、IM、Excel 中，员工差旅需自行查交通/天气/政策，行政人力成本高。本项目用多 Agent 协作重构差旅流程：

1. **行前规划**：自然语言 → 结构化行程（交通/住宿/会议/拜访/餐饮），含出行清单与预算
2. **行中保障**：航班延误/天气/路况主动推送，突发应变重排
3. **行后归档**：行程总结 + 费用统计 + 复用模板
4. **合规闭环**：差旅政策 RAG 检索 → 政策检查 → 审批流转

---

## 三、目录结构

```
corporate-journey-hub/
├── shared/                         # 平台复用层
│   ├── config/settings.py          # 配置管理
│   ├── llm/manager.py              # LLM 多服务商管理器
│   ├── geo/                        # 腾讯地图 / 和风天气客户端
│   ├── middleware/                 # 鉴权 / 会话 / 应用工厂
│   ├── models/                     # 共享数据模型
│   ├── store/                      # JSON 存储基类 + 文档语料库
│   ├── http_client.py              # 服务间调用客户端
│   ├── metrics.py / tracing.py     # Prometheus + 链路追踪
├── services/
│   ├── gateway/                    # 统一网关（单体模式装配三服务）
│   ├── journey-hub/                # 行智 · Agent 编排中心
│   │   ├── api/main.py             # FastAPI 入口 + 登录认证
│   │   ├── state/graph.py          # LangGraph 工作流
│   │   ├── router/intent.py        # 意图路由
│   │   ├── tools/                  # 工具注册 + 处理器
│   │   └── memory/session.py       # 会话管理
│   ├── planner-core/               # 策程 · 行程规划引擎
│   │   ├── api/routers/            # 10 个 APIRouter（行程/审批/报销/报表/政策/指南...）
│   │   ├── generators/             # itinerary / checklist / reroute
│   │   ├── store/                  # 行程/组织/政策/指南存储
│   │   └── archive/summary.py      # 行后归档
│   └── sense-engine/               # 感知 · 实时感知引擎
│       ├── engine.py               # 感知引擎核心（APScheduler 5 分钟轮询）
│       ├── sources/                # 航班/天气/路况/火车/景点
│       └── alerts/manager.py       # 提醒管理
├── frontend/                       # Web 工作台（React 19 + Vite + Tailwind）
├── scripts/                        # 启动/停止/重置/种子数据
├── docs/                           # PRD + PROGRESS + 本文档
└── launcher.py                     # 统一控制台（启动/停止/重启/清数据）
```

---

## 四、技术栈

| 层次 | 技术选型 |
|:---|:---|
| Web 框架 | FastAPI + Uvicorn |
| AI 编排 | LangGraph (StateGraph) |
| LLM 调用 | 腾讯 TokenHub `deepseek-v4-flash`（主）/ `kimi-k3` / `hy-mt2-pro` / `hy-mt2-lite`，OpenAI 兼容多模型适配，管理端配置切换（改默认模型后重启生效） |
| 向量检索 (RAG) | Embedding 可插拔：本地 `BAAI/bge-large-zh-v1.5` 或 OpenAI 兼容；不可用时自动降级关键词检索 |
| 前端 | React 19 + Vite 8 + Tailwind CSS 4 + React Router |
| 定时调度 | APScheduler（5 分钟轮询） |
| 外部数据 | 腾讯位置服务（SN 签名 + MCP 客户端接入）+ 和风天气 + 12306 |
| 可观测性 | Prometheus + 结构化 JSON 日志 + 全链路 Trace |
| 容器化 | Docker + Docker Compose |

> **LLM 通道**：2026-09-17 已全量切换腾讯 TokenHub，主模型 `deepseek-v4-flash`。思考模型（kimi-k3）强制 temperature=1，已在 `shared/llm/manager.py` 按模型名自动处理。

---

## 五、核心功能

| 模块 | 说明 |
|------|------|
| 行前规划 | S0–S6 澄清式对话状态机（澄清≤2轮 → 草案 → 用户确认 → 落库审批），5 场景自适应 |
| 行程生成 | 自然语言 → 结构化行程（交通/住宿/会议/拜访/餐饮），含预算预估；商务类场景**模板直出**（生成阶段 0 次 LLM，典型句式「提取+生成」19.2s → 0ms），personal 走大模型 + 攻略 RAG |
| 出行清单 | 按目的地气候和行程类型自动生成，含证件与公司材料提醒 |
| 实时感知 | 航班延误/天气预警/路况拥堵主动推送（5 分钟轮询，落盘持久化） |
| 应变重排 | 突发情况（延误/闭馆/改期）自动调整后续行程 |
| 政策 RAG | 差旅标准/报销规则语义检索，原文引用作答 |
| 审批流转 | 生成 → 政策检查 → 自动审批 → 门禁闭环 |
| 报销管理 | 提交（自动算额+指派审批人）/ 审批 / 打款 |
| 报表中心 | overview / 按部门 / 按月度 聚合看板 |
| 行程单导出 | 打印友好 HTML，浏览器另存 PDF |
| 景点攻略 | `personal` 场景召回景点语料，与政策语料分开存储 |
| 监控自动串联 | 落库后 `trip_saved` 帧平铺行程要素，前端自动订阅天气 / 路况 / 景点监控 |

---

## 六、架构设计

**三服务架构**：

```
前端工作台 3001 ──▶ 统一网关 8001（单体模式）
                    ├── /        行智 Journey Hub（Agent 编排）
                    ├── /planner 策程 Planner Core（行程规划）
                    └── /sense   感知 Sense Engine（实时监控）
```

微服务模式则三服务各自独立进程占 8001/8002/8003。

**核心设计**：
- **S0–S6 七态状态机** + explicit > 规则正则 > LLM（含 carry 上下文继承）参数提取，商务类场景模板直出（生成阶段 0 LLM，宁慢勿错回退大模型）
- **SSE 结构化帧协议**（progress/chunk/respond/error），进度首帧 0.1s
- **trip_saved 帧平铺行程要素**（origin/destination/dates/attractions/weather/transport），前端收到后自动订阅 sense 监控
- **单/微双形态装配**：别名包 + AsyncExitStack 三 lifespan，业务零复制
- **腾讯地图 SN 签名**：`sig=md5(路径?升序参数+SK)`，lat,lng 与高德相反
- **铁律**：无凭证绝不编造数据，外部 key 空或调用失败一律返回 None

---

## 七、目前进度

| 阶段 | 范围 | 状态 |
|:---|:---|:---|
| P0 · MVP | 行程生成 + 智能问答 + 实时提醒 + Web 工作台 | ✅ 完成 |
| P0+ · 运行形态 | 单体模式（单进程单端口 8001）+ 总控台 + 微服务一键切换 | ✅ 完成 |
| P1 · 企业化 | 差旅政策 RAG + 预算管控 + 审批流转 + 组织人员管理 + 实时数据落地 | ✅ 交付 |
| P2 · 完善 | 报销 / 报表 / 导出 / 景点攻略 / 对话流 v2 / 身份鉴权大修 | ✅ 主体完成 |
| P2 · 阻塞 | SSO（等外部 IdP）/ 真向量 RAG（需开通 embedding 权限） | ⏸ 阻塞 |

**关键里程碑**：
- 2026-09-03：外部服务接入（腾讯地图/和风天气/酒店 POI）
- 2026-09-10：行程规划对话流 v2（澄清式状态机）+ 三服务合并单体模式
- 2026-09-14：启动入口收敛为 start.bat + 停止.bat
- 2026-09-15：身份/鉴权大修 + 自助注册 + 感知监控持久化 + 景点攻略语料
- 2026-09-17：LLM 全量切换腾讯 TokenHub + docs 全量实测校准
- 2026-09-18：商务行程模板直出（生成阶段 0 LLM，「提取+生成」19.2s → 0ms）+ 规则参数抽取 + trip_saved 帧扩展自动订阅 + 出行要素 PRD v1
- 2026-09-20：模板直出预算口径修复（大交通半程估价与每晚住宿入明细，预算 = 明细条目加总 × 人数精确一致；测试基线 203）+ README 与界面截图推送 GitHub
- 2026-09-21：接入腾讯位置服务 MCP（官方 SSE 端点 + mcp SDK，规则路由 6 工具，地图类问答实查注入上下文、失败静默回退；未配 Key 功能关闭；测试基线 219）
- 2026-09-23：配置真实 MCP Key 完成端到端实查验证（地理编码 / POI 搜索 / 驾驶路线全通），补路线类坐标预解析链（geocoder 先行解析 lat,lng 再调路线工具），测试基线 222
- 2026-09-23（下半场）：修复澄清回合死循环 —— 补齐 PRD §6.3 两条缺失规则（「你看着办」= 授权代填；同一行程最多 2 轮反问，超出即终止并引导手动创建），前端整版改版为深色侧边栏数据台并补齐 12 处后端能力入口，测试基线 230

**测试基线**：**230 passed / 0 failed**（15 个测试文件；177 基线 + 模板直出 13 例 + 规则抽取 11 例 + 预算明细一致性回归 2 例 + 地图 MCP 19 例 + 澄清/授权代填 8 例，2026-09-23 实测 28.3s）

---

## 八、踩坑记录

1. **v1 臆造行程（最经典）**：「我下周要去深圳」→ 直接生成「深圳3天文化之旅」+ 自动审批。根因：extract prompt 示例即默认值 + setdefault 兜底 + 无澄清确认 → PRD v2 全链路重写为澄清式状态机（澄清≤2轮 → 草案 → 用户确认 → 才落库审批）。
2. **中文乱码 `æ·±å³`**：requests 对无 charset 的 text/* 按 ISO-8859-1 解码 → 三处补 charset=utf-8 + 历史数据 latin-1 roundtrip 修复。
3. **并发串单**：全局生成器单例被改 model + 共享 `_last_trip` → 每请求独立实例 + 并发回归。
4. **数据孤岛**：.env 相对路径 + 服务各自 cwd → 数据写散 → `_anchor_data_path` 锚定项目根 + 迁移 36 文件。
5. **实时监控"失效"**：订阅/提醒纯内存态重启即空 → 落盘原子写 + RLock + 单行程上限 200 条。
6. **思考模型三类差异**：空 choices 帧 IndexError、思考耗 max_tokens 致空正文（抬到 2048）、JSON 瑕疵重解析。
7. **personal 政策误命中**：非员工 level="" 命中通用政策哑雷 → `_POLICY_EXEMPT_SCENES` 短路。
8. **.env.bak 泄漏**：备份文件推上公开 GitHub → git rm --cached + .gitignore 堵 + key 轮换。
9. **bat 编码**：中文注释/GBK 输出反复出事 → 约定 bat 永远 CRLF + 纯 ASCII。
10. **流式首帧耗时**：旧 bench（MiMo 系）首 chunk 3.6~178s（实测上限 177.4s，2026-09-18 裁定统一写 178），是「流式是必需品不是优化」的论据；切换腾讯 TokenHub 后 hy-mt2-pro 实测首 chunk ~1.1s、全程 3.9~4.2s；进度首帧 0.1s 缓解体感。2026-09-18 进一步结构性提速：商务类场景模板直出后，典型差旅句式「提取+生成」0ms、全链路 0 次 LLM（改造前同句式总耗时 19.2s）。

---

## 九、数据口径与关键指标

> 以下数字均已实测核验，可直接用于简历/面试。**已证伪清单中的数字严禁使用。**

| 指标 | 数值 | 备注 |
|------|------|------|
| 测试用例 | **230 passed / 0 failed** | 15 个测试文件，含 50 并发写、流式错误帧、持久化、政策审批、越权、模板直出、地图 MCP、预算明细一致性、澄清轮次上限 |
| 后端路由 | **82 个端点 / planner-core 10 个 APIRouter** | journey-hub 另有 2 个 |
| 前端页面 | 11 个 | |
| 感知轮询周期 | 5 分钟（`IntervalTrigger(minutes=5)`） | 非 60 秒 |
| SSE 首字响应 | LLM 路径 **~1.1s**（hy-mt2-pro 实测 first chunk），全程 3.9~4.2s；**模板直出路径「提取+生成」0ms** | 进度首帧 0.1s |
| SSE 帧协议 | progress / chunk / respond / **error** | error 帧已落地（journey-hub main.py:268、planner trips.py:244/280/297） |
| 单体冷启动 | 约 6s | |
| 报表 demo 数据 | 94 行程 / ¥274,870 | |

**已证伪勿用（曾出现在旧版简历，已逐条核验证伪）**：
- ❌ 行程变更 60 秒内触达（实为 5 分钟轮询）
- ❌ 线程池化修复异步阻塞（项目内无对应代码）
- ❌ 7 个 APIRouter（planner-core 实为 10 个）
- ❌ 六项端到端自动化验证（实为 230 项测试）

---

## 十、代码盘点与优化（2026-09-18 · 全量颗粒度对齐）

> 盘点口径：全仓每个模块按同一颗粒度测量（LOC / 文件数 / 方法数 / 测试覆盖 / 存储规范 / 重复度），
> 所有数字当日实测，证据标到 `文件:行`。优化项按 P0（正确性）/ P1（结构）/ P2（增强）分级。

---

### 一、总量速览（实测）

| 区域 | 规模 | 说明 |
|:---|:---|:---|
| journey-hub | 17 文件 / 2,829 行 | Agent 编排（graph 568 + handlers 769 + api 330） |
| planner-core | 32 文件 / 5,692 行 | 行程引擎（itinerary 1065 + trips 735 + 10 router + 6 store + 模板直出/规则抽取 2 生成器） |
| sense-engine | 14 文件 / 2,579 行 | 感知（engine 376 + 5 源 + alerts） |
| gateway | 2 文件 / 162 行 | 单体装配层，薄 ✅ |
| shared | 26 文件 / ~2,804 行 | 公共层（llm 642 / models 446 / embedding 315 / store 305） |
| 前端 src | ~4,846 行 | 11 页面 + 4 store + 6 api 模块 + Molecules/Atoms |
| tests | 15 文件 / 2,497 行（根）+ 3 文件（服务内，pytest 收集 27 例） | 基线 230 passed |
| scripts | 10 文件 / 1,788 行 | launcher 配套 + bench + 种子 |

TODO/FIXME/HACK：**0 处** ✅。跨服务无同名业务模块（隔离良好）✅。端点 82 个。

---

### 二、逐模块颗粒度档案

##### planner-core（最重，问题最集中）
| 文件 | 行数 | 结构 | 评价 |
|:---|---:|:---|:---|
| `generators/itinerary.py` | **1065** ⚠️ | ItineraryGenerator **23+ 方法** + 模块级规则函数 | **god file**：混了 4 类职责（见 P1-1）；09-18 模板直出接线再 +74 行，拆分优先级↑ |
| `generators/template.py` | 328 | 商务场景行程骨架直出（09-18 新增） | 职责单一 ✅；时间锚点与预算口径集中 |
| `generators/fast_extract.py` | 266 | 规则正则参数抽取（09-18 新增） | 职责单一 ✅；城市表校验内聚 |
| `api/routers/trips.py` | **735** ⚠️ | 11 端点 + 政策辅助 3 函数 + **150 行 HTML 渲染器**（L567-718） | router 里做渲染，职责越界（P1-2） |
| `store/trip_store.py` | 132 | 手写 dict 存储 | 🔴 **裸写无原子写**（P0-1） |
| `store/profile_store.py` | 163 | 手写 | 🔴 同上，且无 list_all |
| `store/template_store.py` | 167 | 手写 | 🔴 同上 |
| `store/org_store.py` | 232 | 5 个 Store 全继承 BaseJsonStore ✅ | 规范样板 |
| `store/guide_store.py` | — | TravelGuideStore → DocumentCorpusStore → BaseJsonStore ✅ | 间接继承 OK |
| 10 个 router（其余） | 77~201 | 单一领域 | 颗粒度健康 ✅ |

##### journey-hub（编排集中度高）
| 文件 | 行数 | 结构 | 评价 |
|:---|---:|:---|:---|
| `tools/handlers.py` | **769** ⚠️ | ToolHandlers **单类 22 方法** | 五工具 + 6 个 _format_* 私有方法混居（P1-3） |
| `state/graph.py` | 568 | JourneyHubGraph 17 方法 | LangGraph 节点内聚，**拆分收益低，建议不动**（09-18 trip_saved 帧扩展 +36 行） |
| `api/main.py` | 330 | lifespan + 端点 | 🔴 LLM 注册块与 planner 重复（P0-3） |
| `api/auth.py` | 236 | 注册/登录/校验 | 健康 ✅ |

##### sense-engine（同构度最好）
| 文件 | 行数 | 结构 | 评价 |
|:---|---:|:---|:---|
| `engine.py` | 376 | 调度 + 订阅 + 落盘 + RLock | 可接受（09-15 刚修过持久化） |
| `sources/*.py` | 5 源 250~348 | 统一 `check/_fetch_real/_fetch_mock` 签名 + base.py | **同构度好，保持** ✅；train.py 站表逻辑可加缓存 |
| `alerts/manager.py` | 214 | 落盘 + 裁剪 | 健康 ✅ |

##### 前端（颗粒度健康）
- 页面 300~410 行（PolicyPage 410 最大）——React 单页正常区间；
- store 层 4 个单例模式统一（chatStore 385 行）✅；
- api 层 6 模块按域拆分 ✅；
- 结论：**前端不是优化重点**，仅 P2 级可拆子组件。

##### 测试（盲区明确）
| 盲区 | 端点数 | 现状 |
|:---|---:|:---|
| `policy_docs.py` | 7 | **零覆盖** |
| `reimbursements.py` | 6 | **零覆盖** |
| `reports.py` | 3 | **零覆盖** |
| `org.py` | 10 | 约半覆盖（5/10） |

已覆盖：trips 11/11、approvals 6/6、policy 7/7、guides 7/7、templates 5/5、注册、身份隔离、监控持久化、清对话。
另：测试位置分裂（根 `tests/` 15 文件 + 各服务 `tests/`），`pytest.ini testpaths = services tests` 两处都收集，230 例含服务内 27 例。

---

### 三、问题清单（每条带证据）

##### 🔴 P0 —— 正确性 / 真实性

**P0-1｜行程核心数据裸写，简历宣称的原子写只覆盖了一半**
- 证据：`shared/store/base_store.py:112-120` 有 `temp + os.replace` 原子写；但 `trip_store.py:107-113` 是**裸 `open("w") + json.dump`**——行程是全系统核心实体，崩溃即损坏；`profile_store.py`、`template_store.py` 同样裸写。
- 影响：①简历第六节「修复 JSON 存储非原子写损坏问题（temp + os.replace 原子写）」**只对 org 系成立**——2026-09-18 简历与面试素材已同步限定为「BaseJsonStore 体系存储」（技术详解 Q8 附面试标准答案），P0-1 完成后可放开；②行程数据损坏风险真实存在。
- 动作：TripStore / ProfileStore / TemplateStore 迁移到 BaseJsonStore（org 系已有迁移先例，存量数据兼容写迁移函数），预计半天。

**P0-2｜测试盲区 16 端点**
- 证据：policy_docs 7 / reimbursements 6 / reports 3 全部零测试引用（实测 09-18）；org 5/10。
- 影响：P2 功能（报销 / 报表 / 政策文档）改一处坏三处无回归报警；简历「pytest E2E 回归」覆盖面被代码现状限制。
- 动作：补 3 个测试文件（~20 用例，对齐 test_planner_api.py 风格），预计一天。

**P0-3｜LLM 网关注册代码双份**
- 证据：`journey-hub/api/main.py:93-115` 与 `planner-core/api/main.py:75-97` 逐行近似（同一注册块 + 同一 route 循环）；2026-09-17 切 TokenHub 时**同一段代码改了两处**（工作区在途改动实证）。
- 动作：下沉 `shared/llm/bootstrap.py::register_llm_from_settings(llm)`，两服务各留一行调用，预计 1~2 小时。

##### 🟡 P1 —— 结构

**P1-1｜itinerary.py 1065 行拆 4 块**
- 23+ 方法天然分四组：提示词构建（`_build_extraction_prompt` 等）、外部数据 enrich（`_enrich_external/_attach_external` 等 4 个）、JSON 修复（`_safe_json_loads/_repair_json_with_llm/_parse_trip_text`）、演示降级（`_demo_generate/_demo_day_templates/_guess_*`）。
- 动作：拆 `generators/{prompts.py, external_enrich.py, json_repair.py, demo.py}`，ItineraryGenerator 留编排；222 测试兜底回归。预计一天。

**P1-2｜trips.py 735 行拆两块**
- HTML 渲染器（`_render_itinerary_html` L567-718 + `_esc/_fmt_money/_humanize_party`）→ `archive/export_html.py`（导出本就是 archive 域）；政策三辅助（`_match_policy/_policy_preview/_confirm_policy_flow` L96-209）→ `services/policy_service.py`。router 只剩 11 个端点。预计半天。

**P1-3｜handlers.py 769 行单类**
- 6 个 `_format_*` 纯函数 → `tools/formatters.py`；五个工具方法分组留 ToolHandlers。预计半天。

**P1-4｜测试位置统一**
- 根 tests/ 与服务内 tests/ 两套约定并存。建议：服务内测试迁根 `tests/`（对齐现有 13 文件 + conftest），`pytest.ini` 不变。预计 1 小时。

##### 🟢 P2 —— 增强（可缓）

- **P2-1** 前端 PolicyPage（410 行）拆子组件；其余页面不动。
- **P2-2** sense train.py 站表 `_load_station_table` 加进程级缓存（每次实例化重复解析）。
- **P2-3** ~~PROGRESS 失效引用~~ **已完成**：已改指项目文档第七节（2026-09-18 修复）。

---

### 四、建议执行顺序

```
第 1 步（已完成 2026-09-18）提交 TokenHub 切换 3 文件 + docs 重构
第 2 步  P0-3 LLM bootstrap 下沉（1~2h，最小风险热身）
第 3 步  P0-1 store 对齐（半天，带存量迁移 + 回归 222）
第 4 步  P0-2 补 16 端点测试（一天）
第 5 步  P1-1 / P1-2 / P1-3 三个拆分（各半天~一天，每步跑全量测试）
第 6 步  P1-4 测试位置统一（1h）
P2 按需
```

**简历影响**（对齐完成后）：
- 「修复 JSON 并发写坏（temp + os.replace）」→ 全 store 成立，可放开写；
- 「pytest E2E 回归」可扩展为「生成 / 越权 / 政策审批 / 流式 / 报销 / 报表」；
- 「单体路由按领域拆分 10 个 APIRouter」不变，可补「生成器按提示词 / 外部数据 / JSON 修复分层」。

---

## 十一、已知限制与待办

**已知限制**：
- 感知监控为 5 分钟轮询，非实时推送
- 真向量 RAG 未启用（当前关键词检索降级），需开通 embedding 权限或装 sentence-transformers
- SSO 未接入（等外部 IdP）
- 服务端会话历史为进程内，重启即空（跨刷新靠 localStorage 快照）

**待办**：
- [ ] TripStore / ProfileStore / TemplateStore 原子写对齐（迁移 BaseJsonStore，代码盘点 P0-1）
- [ ] 出行要素产品闭环 PRD Phase 1（首页出行卡 / 提醒推送，见 `docs/PRD.md`）
- [ ] SSO（OIDC）接入
- [ ] 真向量 RAG 启用（`BAAI/bge-large-zh-v1.5`，1024 维）
- [ ] 报表加时间范围过滤 + 导出 CSV/Excel
- [ ] 报销按费用类目透视
- [ ] 服务端历史持久化（接 DB）

---

## 快速开始

详见 [`README.md`](../README.md)。核心入口：

```bash
python launcher.py            # 默认单体启动：单进程单端口 8001
python launcher.py micro      # 微服务模式（8001/8002/8003）
python launcher.py stop       # 停止全部
```

或直接双击 `start.bat`（启动）/ `停止.bat`（停止）。
